---
version: 4
datadir: data
hierarchy:
  - name: "Platform"
    backend: yaml
    paths:
      - "osfamily/%{facts.os.name}/%{facts.os.release.major}"
      - "osfamily/%{facts.os.family}"
      - "common"
